import "../static/styles/styles.scss";
const Layout = props => {
    return (
        <div className="page-wrapper">
            {props.children}
        </div>
    )
  }
  
  export default Layout
  